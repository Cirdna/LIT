# Core: hosted (OpenRouter) backend path.
pymupdf>=1.24
requests>=2.31
pytesseract>=0.3.10
pillow>=10.0
rapidfuzz>=3.6
pydantic>=2.6
playwright>=1.42
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Local VLM backends only (qwen / internvl) -- ~2.5GB, skip for hosted use.
# pip install -e ".[local]"
# torch>=2.2
# torchvision>=0.17
# transformers>=4.49
# accelerate>=0.30
# qwen-vl-utils>=0.0.8

# Dev
pytest>=8.0
pytest-cov>=5.0
